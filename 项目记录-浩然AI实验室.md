# 浩然AI实验室｜项目记录

更新时间：2026-09-16

## 产品定位

面向想用 Vibe Coding 制作网站的初学者。核心价值不是售卖组件数量，而是把模糊的网站想法整理为 AI 可以直接执行的建站方案。

主站应只有四个明确入口：

1. 主页：介绍产品、目标用户、使用路径和价值。
2. 组件库：所有人公开浏览；查看组件类别、用途和样式，不涉及配置、额度或 AI 聊天。
3. 配置器：用户点选组件、看预览、生成完整 Prompt。
4. 年度版权益：解释免费路径与完整工作流的区别，并承接付费。

## 当前权益逻辑

- 未登录：浏览主页与组件库；进入配置器时引导登录。
- 登录免费用户：可使用基础 6 类（页面结构、导航与切换、常用组件、滚动与动作、提示与加载、视觉风格）；完整方案生成与 AI 顾问共 10 次试用。
- 年度版：解锁全部 10 类、不限次数生成与复制 Prompt、不限次数 AI 顾问。
- 浏览、点选和预览不扣试用次数。
- 配置器需要保留“清空当前选择”，且只清选择、不清项目名称和目标。

## 现有文件

- `index.html`：当前干净主站壳，包含主页、组件库、配置器和年度版权益四个视图。
- `components.js`：10 个类别和组件内容数据。
- `configurator-legacy.html`：旧配置器备份，用于保留历史实现，不应再作为主站 UI 继续迭代。
- `auth.js`、`chat.js`：登录、权益和 AI 聊天相关逻辑。
- `library.html`、`showcase.html`：旧页面，暂不作为主导航入口。

## 当前进度

- 已把品牌名调整为“浩然AI实验室”，品牌图标使用 `H`。
- 已清理原 `index.html` 多套历史 CSS 互相覆盖造成的空白页问题，改为干净的主站结构。
- 组件库已具备“类别展开 → 查看完整组件”的基础数据渲染。
- 配置器已迁入主站，具备点选、预览、健康度、清空选择和生成 Prompt 的基础结构。

## 当前视觉问题

- 不使用 Emoji 作为正式 UI 图标；它们显得廉价且风格不一致。
- 后续应统一接入一套线性 SVG 图标库，优先建议 Lucide Icons；也可选择 Iconoir 或 Phosphor Icons。
- 在确定图标库和参考方向前，不继续做视觉重构。
- 不再向页面堆叠临时 `<style>`；后续拆分设计令牌、布局和组件样式。

## 接下来：内容制作优先

先完善网站内容，而不是继续修改 UI：

1. 定义 10 大组件类别的用户语言、用途、适用场景和选择建议。
2. 补齐每个组件的“什么时候用 / 不该什么时候用 / 给 AI 的提示词要点”。
3. 为配置器准备常见网站目标的推荐方案与说明。
4. 完善主页文案：目标用户、工作流、免费与年度版权益、常见问题。
5. 再确定图标库与视觉参考，统一升级 UI。

## Vibe Coding 经验

先定信息架构和设计系统，再写页面；每次只采用一个主要参考方向；用明确的验收标准约束 AI，例如统一图标库、颜色数量、按钮规格、响应式规则、文字对比度和无 Emoji 要求。
